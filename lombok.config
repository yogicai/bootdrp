#lombok 默认对 boolean 类型字段生成的 get 方法使用 is 前缀, 通过此配置则使用 get 前缀
#lombok.getter.noIsPrefix=true
#默认的 set方法返回 void 设置为 true 返回调用对象本身
lombok.accessors.chain=false
#ToString EqualsAndHashCode方法包含父类属性
lombok.equalsandhashcode.callsuper=call